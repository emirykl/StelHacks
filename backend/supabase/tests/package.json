{
  "name": "@stelhacks/db-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "What each role can actually reach, checked over the wire rather than read in a policy",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.47.0"
  },
  "devDependencies": {
    "dotenv": "^16.4.7",
    "typescript": "^5.6.2",
    "vitest": "^2.1.8"
  }
}
